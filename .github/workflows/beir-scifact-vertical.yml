name: BEIR SciFact Vertical

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archived workflow
        run: |
          echo "Archived: old SciFact vertical workflow is disabled for push."
          echo "Use preserved results under results/beir/scifact/."
