name: BEIR Summary

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archived workflow
        run: |
          echo "Archived: BEIR summary is preserved in results/beir/summary.md."
          echo "Refresh manually only after benchmark result files are intentionally updated."
