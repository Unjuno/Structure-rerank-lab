name: Real-like Expanded

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archived workflow
        run: |
          echo "Archived: real-like expanded experiments are preserved in results/ and docs/current-state.md."
          echo "Do not run this on every push. Re-enable only when intentionally refreshing the benchmark."
