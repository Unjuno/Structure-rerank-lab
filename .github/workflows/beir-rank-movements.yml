name: BEIR Rank Movements

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archived workflow
        run: |
          echo "Archived: BEIR rank movement outputs are preserved under results/beir/*/."
          echo "Refresh deliberately from an experiment branch only."
