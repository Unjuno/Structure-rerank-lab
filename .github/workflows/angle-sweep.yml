name: Angle Sweep

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archived workflow
        run: |
          echo "Archived: angle sweep results are recorded under results/ when refreshed intentionally."
          echo "Do not run this on every push."
