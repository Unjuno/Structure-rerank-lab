name: Angle Sweep

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archived workflow
        run: |
          echo "Archived: angle sweep code remains in structure_rerank/angle_sweep.py."
          echo "Do not run on every push; refresh deliberately from a separate branch when needed."
