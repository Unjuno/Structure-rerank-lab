name: BEIR ArguAna Vertical

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archived workflow
        run: |
          echo "Archived: ArguAna vertical results are preserved under results/beir/arguana/."
          echo "Refresh deliberately from an experiment branch only."
