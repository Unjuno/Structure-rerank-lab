name: SciFact V2

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archived workflow
        run: |
          echo "Archived: SciFact vertical results are preserved under results/beir/scifact/."
          echo "Use a temporary refresh workflow or local branch only when intentionally recomputing."
