name: ArguAna Corpus Vertical

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archived workflow
        run: |
          echo "Archived: ArguAna corpus-vertical diagnostic workflow is no longer run on push."
          echo "Corpus-derived vertical values are experimental and recorded as missing in docs/current-state.md."
