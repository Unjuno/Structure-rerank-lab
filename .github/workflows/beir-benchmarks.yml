name: BEIR Benchmarks

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archived workflow
        run: |
          echo "Archived: BEIR benchmark outputs are preserved under results/beir/."
          echo "Use archived commands/docs to refresh deliberately; do not run on every push."
