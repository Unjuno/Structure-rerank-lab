name: BEIR NFCorpus Vertical

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archived workflow
        run: |
          echo "Archived: NFCorpus vertical results are preserved under results/beir/nfcorpus/."
          echo "Use a temporary refresh workflow or local branch only when intentionally recomputing."
